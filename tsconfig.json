{
  "compilerOptions": {
    "target": "ES6",                              // Define o alvo para ES6
    "module": "commonjs",                         // Usa o sistema de módulos CommonJS
    "strict": true,                               // Ativa todas as verificações de tipo estritas
    "esModuleInterop": true,                      // Habilita a interoperabilidade com módulos ES
    "skipLibCheck": true,                         // Ignora a verificação de tipo dos arquivos de declaração
    "forceConsistentCasingInFileNames": true,     // Força a consistência no uso de maiúsculas/minúsculas
    "outDir": "./dist",                           // Define o diretório de saída para o código compilado
    "rootDir": "./src",                           // Define o diretório raiz dos arquivos TypeScript
    "resolveJsonModule": true,                    // Permite a importação de arquivos JSON
    "strictPropertyInitialization": false         // Permite inicialização laxa de propriedades (útil com Sequelize)
  },
  "include": [
    "src/**/*",                                   // Inclui todos os arquivos TypeScript na pasta src
    "config/**/*.json"                            // Inclui arquivos JSON na pasta config
  ],
  "exclude": [
    "node_modules"                                // Exclui a pasta node_modules
  ]
}
